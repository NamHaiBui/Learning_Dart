void main(List<String> args) {
  /// if you are working with jsut the value of null, and you don't actually have a data type for it. dart will understand that it is dynamic.
  const someValue = null;
  print(someValue);
}
